using System.Threading.Tasks;
using NUnit.Framework;

namespace FinancialApi.IntegrationTests.Controller
{
    public class FinancialControlerTests
    {

        [Test]
        public async Task Receipt_ReturnsStausCodeBadRequest_WithEmptyDescription()

        {
            // FAKE HTTP GET "/620"
            //IActionResult result = await pageModel.OnGetAsync(id:620, path:"");
 
            //RedirectResult r = Assert.IsType<RedirectResult>(result);
            //Assert.NotNull(r);
            //Assert.True(r.Permanent); //HTTP 301?
            //Assert.Equals("/620/jessica-rose-and-the-worst-advice-ever",r.Url);
        } 
    }
}